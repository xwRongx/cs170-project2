#include "../headers/Node.h"


void Node::displayNode(){
//Display: outputs “Using feature(s) {” + features + “}” + “ accuracy is ” + node.accuracy + “%”
    std::cout << "Using feature(s) {";
    //for feature in features
    //+ features + “}” + “ accuracy is ” + node.accuracy + “%”";
}
